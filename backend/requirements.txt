# Direct dependencies — pinned to exact versions for reproducibility.
# Run `pip install -r requirements.txt` inside the container or venv.
# To upgrade: change the version here, rebuild the image, test.

fastapi==0.135.3
asyncpg==0.30.0
uvicorn[standard]==0.44.0
sqlalchemy[asyncio]==2.0.49
alembic==1.18.4
aiosqlite==0.22.1
anthropic==0.94.0
langgraph==1.1.6
langchain-anthropic==1.4.0
langchain-core==1.2.28
mcp==1.27.0
pydantic==2.12.5
pydantic-settings==2.13.1
python-dotenv==1.2.2
httpx==0.28.1
sse-starlette==3.3.4
